apiVersion: v1
kind: ConfigMap
metadata:
  name: gitops-repo
data:
  branch: main
  host: github.com
  protocol: https
  repo: my-app-2-manifest
  org: Hibiki-Ito2
  parentdir: bash -c 'echo qa/$(basename ${NAMESPACE} -dev)'
